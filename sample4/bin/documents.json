{
  "_id" : "_design/documents",
  "views" : {
    "all" : {
      "map" : "   function(doc) {
                    var type, key;
                    if (doc.key && doc.type && doc.df_type === 'document') {
                    	emit(key);
                    }
                  }"
    
    }
  }
}