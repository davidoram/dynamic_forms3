{
  "_id" : "_design/schemas",
  "views" : {
    "all" : {
      "map" : "   function(doc) {
                    var type, key;
                    if (doc.key && doc.type && doc.df_type === 'schema') {
                    	emit(key);
                    }
                  }"
    
    }
  }
}